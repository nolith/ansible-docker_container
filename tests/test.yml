---
- hosts: localhost
  remote_user: root
  # roles:
  #   - role: docker_container
  #     container_name: test
  #     docker_image: quay.io/nolith/test
  tasks:
    - debug: msg="System {{ inventory_hostname }}"
    - debug: var={{hostvars}}
